# datasets.yaml for YOLOv5 training with multiple datasets

path: /kaggle/working/yolov5_object_segm/datasets/person_dataset  # update this to the correct Kaggle path

train: train/images
val: valid/images
test: test/images

# Number of classes
nc: 1  

# Class names
names: ['person']  
