# datasets.yaml for YOLOv5 training with multiple datasets

path: /kaggle/working/datasets/person_dataset
train: train/images
val: valid/images
test: test/images

# Number of classes
nc: 1  

# Class names
names: ['person']  
